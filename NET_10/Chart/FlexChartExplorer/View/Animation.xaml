<UserControl
    x:Class="FlexChartExplorer.Animation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexChartExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c1="using:C1.WinUI.Chart"
    xmlns:res="using:FlexChartExplorer.Resources"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10 0">
            <ComboBox x:Name="cbChartType" Header="{x:Bind res:AppResources.ChartType}" Width="180" Margin="10"
                      ItemsSource="{Binding DataContext.ChartTypes}"
                      SelectedIndex="0">
            </ComboBox>
            <AppBarButton Icon="Refresh" Click="NewData" Label="{x:Bind res:AppResources.NewData}" />
            <AppBarButton Icon="Edit" Click="UpdateData" Label="{x:Bind res:AppResources.UpdateData}" />
            <AppBarButton Icon="Add" Click="AddSeries" Label="{x:Bind res:AppResources.AddSeries}" />
            <AppBarButton Icon="Remove" Click="RemoveSeries" Label="{x:Bind res:AppResources.RemoveSeries}" />
            <AppBarButton Icon="Add" Click="AddPoint" Label="{x:Bind res:AppResources.AddPoint}" />
            <AppBarButton Icon="Remove" Click="RemovePoint" Label="{x:Bind res:AppResources.RemovePoint}" />
        </StackPanel>
        <c1:FlexChart x:Name="flexChart" Loaded="ChartLoaded" AnimationSettings="All" RenderMode="Direct2D"
                      ChartType="{Binding ElementName=cbChartType, Path=SelectedValue}"
                      BindingX="X" Binding="Y" Grid.Row="1">
        </c1:FlexChart>
    </Grid>
</UserControl>
