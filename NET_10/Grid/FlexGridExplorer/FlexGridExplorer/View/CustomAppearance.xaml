<UserControl
    x:Class="FlexGridExplorer.CustomAppearance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c1="using:C1.WinUI.Grid"
    xmlns:core="using:C1.WinUI.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:res="using:FlexGridExplorer.Resources"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/GridThemes.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="4">
            <TextBlock Text="{x:Bind res:AppResources.Theme}" VerticalAlignment="Center"/>
            <ComboBox x:Name="Themes" Margin="4 0 0 0" SelectionChanged="OnThemeChanged"/>
        </StackPanel>
        <c1:FlexGrid x:Name="grid" Margin="12" Grid.Row="1" Style="{StaticResource Material}" SelectionMode="RowRange"/>
    </Grid>
</UserControl>
