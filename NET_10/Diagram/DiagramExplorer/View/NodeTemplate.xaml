<UserControl
    x:Class="DiagramExplorer.NodeTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiagramExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c1="using:C1.WinUI.Diagram"
    xmlns:res="using:DiagramExplorer.Resources"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="ListBox">
            <Setter Property="MaxHeight" Value="120" />
            <Setter Property="Width" Value="180" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="Expander">
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Width" Value="200" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <c1:FlexDiagram x:Name="diagram" Direction="TopBottom" ScaleMode="ScaleToFit" >
            <c1:FlexDiagram.NodeTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="3">
                        <StackPanel Margin="8">
                            <TextBlock Text="{Binding}" FontSize="12" FontWeight="Bold" />
                            <Expander Header="Properties" Visibility="{Binding HasProperties}">
                                <ListBox ItemsSource="{Binding Properties}" />
                            </Expander>
                            <Expander Header="Events" Visibility="{Binding HasEvents}">
                                <ListBox ItemsSource="{Binding Events}" />
                            </Expander>
                            <Expander Header="Methods" Visibility="{Binding HasMethods}">
                                <ListBox ItemsSource="{Binding Methods}" />
                            </Expander>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </c1:FlexDiagram.NodeTemplate>
        </c1:FlexDiagram>
    </Grid>
</UserControl>
