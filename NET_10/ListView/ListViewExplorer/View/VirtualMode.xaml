<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ListViewExplorer.VirtualMode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ListViewExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:listView="using:C1.WinUI.ListView"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="listView:C1ListView">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="listView:ListViewItemView">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                        <Setter Property="MinHeight" Value="30" />
                        <Setter Property="BorderBrush" Value="#5f6fa4" />
                        <Setter Property="BorderThickness" Value="0 0 0 1" />
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <listView:C1ListView x:Name="listView" Grid.Row="0" SelectionMode="Single" DisplayMemberPath="Name"  Margin="10 0 10 10" IsSwipeEnabled="True"/>
    </Grid>
</UserControl>
