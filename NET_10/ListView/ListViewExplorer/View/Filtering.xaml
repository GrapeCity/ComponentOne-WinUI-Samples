<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ListViewExplorer.Filtering"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ListViewExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:input="using:C1.WinUI.Input"
    xmlns:listView="using:C1.WinUI.ListView"
    xmlns:resources="using:ListViewExplorer.Resources"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <input:C1TextBox x:Name="txtFilter" TextChanged="C1TextBox_TextChanged"  Placeholder="{x:Bind resources:AppResources.Placeholder}" 
                      Grid.Row="0" Margin="10 0 0 10" Width="300" HorizontalAlignment="Left"/>

        <listView:C1ListView x:Name="listView" Grid.Row="1" Grid.ColumnSpan="4" SelectionMode="Single" DisplayMemberPath="Name"
                       Margin="10 0 10 10" ItemHeight="30" SelectedBackground="#99c9ef" />

    </Grid>
</UserControl>
