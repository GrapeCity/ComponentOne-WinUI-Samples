<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GaugeExplorer.DisplayingValues"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GaugeExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:C1.WinUI.Core"
    xmlns:input="using:C1.WinUI.Input"
    xmlns:gauge="using:C1.WinUI.Gauge"
    xmlns:res="using:GaugeExplorer.Resources"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock x:Name="lblShowText" Text="Show text" VerticalAlignment="Center"/>
            <ComboBox x:Name="showItemsPicker" ItemsSource="{Binding ShowTextItems}" SelectedIndex="{Binding ShowTextSelectedIndex,Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
            <TextBlock x:Name="lblValue" Text="Value" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <input:C1NumericBox Minimum="{Binding Min}" Maximum="{Binding Max}" Value="{Binding Value,Mode=TwoWay}" Increment="25" HorizontalAlignment="Center" Format="P0" />
        </StackPanel>
        <gauge:C1LinearGauge IsEnabled="True" Minimum="{Binding Min}" Maximum="{Binding Max}" Value="{Binding Value,Mode=TwoWay}" Grid.Row="1"
                          Step="{Binding Step}" Format="{Binding Format}" TextVisibility="{Binding ShowTextViaSelectedIndex}" 
                          ShowRanges="{Binding ShowRanges}" IsReadOnly="False" >
            <gauge:GaugeRange From="0" To="40" Brush="#22B14C"/>
            <gauge:GaugeRange From="40" To="80" Brush="#FF8080"/>
            <gauge:GaugeRange From="80" To="100" Brush="#00A2E8"/>
        </gauge:C1LinearGauge>
        <gauge:C1RadialGauge IsEnabled="True" Minimum="{Binding Min}" Maximum="{Binding Max}" Value="{Binding Value,Mode=TwoWay}" Grid.Row="2"
                          Step="{Binding Step}" Format="{Binding Format}" TextVisibility="{Binding ShowTextViaSelectedIndex}" 
                          ShowRanges="{Binding ShowRanges}" IsReadOnly="False" AutoScale="True" VerticalAlignment="Top" >
            <gauge:GaugeRange From="0" To="40" Brush="#22B14C"/>
            <gauge:GaugeRange From="40" To="80" Brush="#FF8080"/>
            <gauge:GaugeRange From="80" To="100" Brush="#00A2E8"/>
        </gauge:C1RadialGauge>
    </Grid>
</UserControl>
