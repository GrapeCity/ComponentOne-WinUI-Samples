<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GaugeExplorer.UsingRanges"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GaugeExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:C1.WinUI.Core"
    xmlns:input="using:C1.WinUI.Input"
    xmlns:gauge="using:C1.WinUI.Gauge"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <core:C1CheckBox IsChecked="{Binding ShowRanges,Mode=TwoWay}" VerticalAlignment="Stretch"/>
            <TextBlock x:Name="lblShowRanges" Text="Show ranges" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
            <TextBlock x:Name="lblValue" Text="Value" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <input:C1NumericBox Value="{Binding Value,Mode=TwoWay}" Increment="25" HorizontalAlignment="Center"/>
        </StackPanel>
        <gauge:C1LinearGauge x:Name="linearGauge" Value="{Binding Value,Mode=TwoWay}" ShowRanges="{Binding ShowRanges}" IsReadOnly="False" Grid.Row="1">
            <gauge:GaugeRange From="0" To="40" Brush="{ThemeResource SystemFillColorSuccessBrush}"/>
            <gauge:GaugeRange From="40" To="80" Brush="{ThemeResource SystemFillColorCriticalBrush}"/>
            <gauge:GaugeRange From="80" To="100" Brush="{ThemeResource SystemFillColorCautionBrush}"/>
        </gauge:C1LinearGauge>
        <gauge:C1RadialGauge x:Name="radialGauge" Value="{Binding Value,Mode=TwoWay}" ShowRanges="{Binding ShowRanges}" Grid.Row="2"
                          IsReadOnly="False" AutoScale="True" VerticalAlignment="Top">
            <gauge:GaugeRange From="0" To="40" Brush="{ThemeResource SystemFillColorSuccessBrush}"/>
            <gauge:GaugeRange From="40" To="80" Brush="{ThemeResource SystemFillColorCriticalBrush}"/>
            <gauge:GaugeRange From="80" To="100" Brush="{ThemeResource SystemFillColorCautionBrush}"/>
        </gauge:C1RadialGauge>
    </Grid>
</UserControl>
