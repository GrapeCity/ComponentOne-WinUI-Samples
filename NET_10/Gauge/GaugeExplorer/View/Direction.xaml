<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GaugeExplorer.Direction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GaugeExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:C1.WinUI.Core"
    xmlns:input="using:C1.WinUI.Input"
    xmlns:gauge="using:C1.WinUI.Gauge"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock x:Name="lblDir" Text="Direction" VerticalAlignment="Center"/>
            <ComboBox x:Name="directionPicker" ItemsSource="{Binding DirectionItems}" SelectedIndex="{Binding DirectionSelectedIndex,Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        </StackPanel>
        <StackPanel Orientation="{Binding DirectionOrientation}" VerticalAlignment="Center" Grid.Row="1">
            <gauge:C1LinearGauge Value="{Binding Value,Mode=TwoWay}" Direction="{Binding Direction}" TextVisibility="All" IsReadOnly="False" />
            <gauge:C1BulletGraph Value="{Binding Value,Mode=TwoWay}" Direction="{Binding Direction}" TextVisibility="All" IsReadOnly="False" Good="80" Bad="45" Target="90" />
        </StackPanel>
    </Grid>
</UserControl>
