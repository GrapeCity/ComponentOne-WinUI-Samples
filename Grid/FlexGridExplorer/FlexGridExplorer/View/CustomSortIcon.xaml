<UserControl
    x:Class="FlexGridExplorer.CustomSortIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c1="using:C1.WinUI.Grid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="2">
            <TextBlock x:Name="lblIconPos" Text="Sort Icon Position" VerticalAlignment="Center" Margin="10 0"/>
            <ComboBox x:Name="sortIconPosition" SelectionChanged="SortIconPositionChanged"/>

            <TextBlock x:Name="lblIconTemplate" Text="Sort Icon Template" VerticalAlignment="Center" Margin="10 0"/>
            <ComboBox x:Name="sortIconTemplate" SelectionChanged="SortIconTemplateChanged"/>
        </StackPanel>
        <c1:FlexGrid x:Name="grid" SortIconPosition="Left" Grid.Row="2"/>
    </Grid>
</UserControl>
